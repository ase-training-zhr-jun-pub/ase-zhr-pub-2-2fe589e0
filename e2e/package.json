{
  "name": "calvin-e2e",
  "private": true,
  "description": "End-to-End-Tests (Playwright) für den Calvin-Frontend-Prototyp",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
